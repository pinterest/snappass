version: '2'

services:

    snappass:
        build: .
        image: pinterest/snappass
        ports:
            - "5000:5000"
        stop_signal: SIGINT
        environment:
            - REDIS_HOST=redis
        depends_on:
          - redis

    redis:
        image: "redis:latest"
